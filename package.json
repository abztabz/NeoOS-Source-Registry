{
  "name": "@neoos/source-registry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Governed source registry and provider-independent data gateway for NeoOS products",
  "engines": { "node": ">=22" },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
