# SERP intelligence

SERP providers are experimental. Normalized output is limited to discovery metadata such as ranking positions, result titles/URLs/domains and related-search structures. Coarse result counts are not search volume or keyword difficulty. Snippet/body reuse is deliberately excluded from the shared contract.
