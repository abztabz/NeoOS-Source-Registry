# Attribution

Required source attribution is registry metadata and is returned in gateway provenance where applicable. Consumers must preserve required attribution when using provider-derived data and must not assume that attribution alone grants rights to linked third-party content.
