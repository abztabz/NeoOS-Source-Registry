# Freshness

Each provider records a freshness policy because 'current' means different things for news, filings, economic series, DNS, weather and market data. Consumers must interpret timestamps according to the capability and must not turn stale discovery metadata into a current factual claim.
