# Public API catalogues

Community API directories such as public-apis/public-apis are discovery inputs only. Entries must pass NeoOS governance review before entering this registry, and registry inclusion still records a lifecycle status rather than assuming production eligibility.
