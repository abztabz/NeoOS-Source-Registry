# Vendor independence

The registry's purpose is not to collect vendor integrations. It lets products express needs in stable capability language while provider choice remains replaceable. This keeps product architecture stable when an API disappears, becomes paid, changes terms, degrades or is superseded.
