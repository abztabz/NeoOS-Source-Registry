# API surface

The stable logical surface consists of capability definitions, governed provider metadata, registry-first policy resolution, safe registry snapshots, the Neo Data Gateway request contract and normalized provider adapters. Consumers should not depend on provider-specific response shapes.
