# Query boundaries

Adapters bound and normalize query strings before provider requests. Consumers should provide public search concepts rather than raw documents or user records unless a future capability explicitly defines and governs that data class.
