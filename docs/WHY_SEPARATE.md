# Why this repository is separate

The Source Registry serves multiple NeoOS products and therefore cannot be governed by the lifecycle of one consumer. Separating it prevents NeoContent-specific release pressure from changing shared source policy, allows NeoCRM/Wealth/Living Website to reuse the same capability decisions, and creates one place to retire or replace providers without rebuilding every product's architecture.
