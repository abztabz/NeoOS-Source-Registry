# Consumer strictness

A consumer can refuse a registry-approved source for its own stricter product requirements. It cannot locally override a shared block, commercial restriction or privacy boundary. Shared governance is a floor, not a ceiling.
