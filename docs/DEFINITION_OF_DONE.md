# Definition of done

The standalone extraction is governance-complete when the provider/capability catalogues, gateway contract, current live adapters, security boundaries and tests are owned here and CI passes. Full physical cutover is complete only when NeoContent consumes a versioned shared distribution, its preview/production deployments pass, and its vendored compatibility copy is removed.
