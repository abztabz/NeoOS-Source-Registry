# No secrets

Never commit API keys, tokens, customer credentials or provider account identifiers here. `secretEnvName` records only the name a consuming server should use to inject a credential. Safe snapshots deliberately omit even those names.
