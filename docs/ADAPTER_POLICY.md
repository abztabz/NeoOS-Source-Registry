# Adapter policy

An adapter normalizes a provider; it does not grant eligibility. Registry status remains authoritative. Planned adapters are implemented only when a consumer needs the capability. This avoids accumulating untested vendor code and keeps the registry complete without pretending every possible provider integration is built.
