# Future package distribution

If consumed as a package, publish/pin immutable versions and let each product upgrade intentionally. Do not depend directly on a moving branch in production. Consumer lockfiles should make the exact registry version auditable.
