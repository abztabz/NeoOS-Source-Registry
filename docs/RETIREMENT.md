# Provider retirement

When a provider becomes unsuitable, mark it retired or blocked centrally rather than deleting the history or patching each product. Consumers that request the capability should then fall through to another eligible provider or receive an explicit unavailable result.
