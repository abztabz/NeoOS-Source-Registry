# Observability

Shared gateway telemetry should record provider ID, outcome, duration, item count/fallback behavior and timestamps without logging customer query text, article content, credentials or raw provider error messages. Consumers can attach their own correlation identifiers outside the provider payload.
