# Data minimization

Registry presence does not authorize arbitrary outbound data. Consumers should send only the minimum public query metadata required for the capability. Customer content, CRM personal information, portfolio/account data, precise private location and credentials require a separate product-specific justification and privacy path; most public-data capabilities should never receive them.
