# NeoOS integration rule

For any NeoOS project, treat external-data sourcing as a shared infrastructure concern by default. The project identifies what it needs; this registry determines whether a governed capability/provider path already exists. This rule applies even when the project did not explicitly ask for an API or mention the registry.
